# chart_patterns 전용 의존성 (프로젝트 requirements.txt 와 별개로 설치 가능)
# pip install -r chart_patterns/requirements.txt
numpy>=1.24
pandas>=2.0
requests>=2.28
python-dotenv>=1.0
yfinance>=0.2.40          # 미장 시세
PyMuPDF>=1.24             # PDF 파싱 — book_patterns.json 이 이미 있으면 불필요

# API 서버로 띄울 때만 (python -m chart_patterns.api)
fastapi>=0.110
uvicorn>=0.27
